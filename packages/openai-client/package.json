{
  "name": "@traain/openai-client",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "lint:fix": "pnpm lint --fix",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@traain/db": "^0.1.0",
    "openai": "^3.2.1",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@traain/eslint-config": "^0.1.0",
    "eslint": "^8.38.0",
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  }
}
